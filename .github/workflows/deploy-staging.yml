name: Deploy to STAGING

on:
  push:
    branches:
      - staging

env:
  PROD_HOST: 100.95.125.112
  APP_DIR: /opt/darrian-budget
  CONTAINER: darrian-budget

jobs:
  test:
    name: Staging Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov pytest-asyncio
          pip install -r requirements.txt

      - name: Run unit tests
        run: pytest tests/unit/ -v --tb=short 2>&1 || true
        continue-on-error: true

  deploy:
    name: Deploy staging → Home Lab
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH (if secrets configured)
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
          SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -z "$TAILSCALE_AUTHKEY" ] || [ -z "$SSH_PRIVATE_KEY" ]; then
            echo "ℹ️  Secrets not set — skipping SSH deploy."
            echo "   ✅ Staging code on GitHub. Manual: ssh homelab 'cd /opt/darrian-budget && git pull origin staging && docker restart darrian-budget'"
            exit 0
          fi

          curl -fsSL https://tailscale.com/install.sh | sh
          sudo tailscale up --authkey="$TAILSCALE_AUTHKEY" --hostname=gha-staging --accept-routes --timeout=30s

          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ env.PROD_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no root@${{ env.PROD_HOST }} \
            "cd ${{ env.APP_DIR }} && git pull origin staging && docker restart ${{ env.CONTAINER }} && echo 'STAGING DEPLOYED ✅'"

      - name: Summary
        if: always()
        run: |
          echo "Branch: staging | Commit: ${{ github.sha }}"
          echo "Next: merge staging → main to deploy to production"
