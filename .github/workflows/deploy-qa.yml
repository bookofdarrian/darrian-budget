name: Deploy to QA

on:
  push:
    branches:
      - qa

env:
  APP_DIR: /opt/darrian-budget
  CONTAINER: darrian-budget

jobs:
  test:
    name: QA Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install pytest pytest-asyncio && pip install -r requirements.txt
      - run: pytest tests/unit/ -v --tb=short 2>&1 || true
        continue-on-error: true

  deploy:
    name: Deploy qa → Home Lab
    runs-on: self-hosted
    needs: test
    steps:
      - name: Pull latest code
        run: |
          cd ${{ env.APP_DIR }}
          git fetch origin
          git reset --hard origin/qa
          git checkout qa
          git pull origin qa

      - name: Restart container
        run: sudo docker restart ${{ env.CONTAINER }}

      - name: Health check
        run: |
          sleep 8
          curl -sf http://localhost:8501/_stcore/health && echo "✅ QA is healthy" || echo "⚠️ Still starting..."
        continue-on-error: true

      - name: Summary
        if: always()
        run: echo "QA deployed — commit ${{ github.sha }} | Next: promote qa → staging"
